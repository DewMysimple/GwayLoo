---
type: state
status: active
kind: process
importance: medium
updated: 2026-08-25
topic: current-todos
source_logs:
  - "[[日志/2026-08-20-工程记忆初始化|工程记忆初始化]]"
  - "[[日志/2026-08-24-主线与Branch复刻架构差距比较|主线与 Branch 复刻架构差距比较]]"
  - "[[日志/2026-08-24-纸片世界朝向与Fade显现对齐|纸片世界朝向与 Fade 显现对齐]]"
  - "[[日志/2026-08-24-FullPaint显现与生命周期对齐|Full Paint 显现与生命周期对齐]]"
  - "[[日志/2026-08-24-Ground与渲染分辨率契约对齐|Ground 与渲染分辨率契约对齐]]"
  - "[[日志/2026-08-24-背景合成与阴影职责对齐|背景合成与阴影职责对齐]]"
  - "[[日志/2026-08-24-运行时初始化与流体散度对齐|运行时初始化与流体散度对齐]]"
  - "[[日志/2026-08-24-流体实例时间步长对齐|流体实例时间步长对齐]]"
  - "[[日志/2026-08-24-降级页面静态交互边界|降级页面静态交互边界]]"
  - "[[日志/2026-08-24-流体实例活跃生命周期对齐|流体实例活跃生命周期对齐]]"
  - "[[日志/2026-08-24-流体椭圆笔触SDF对齐|流体椭圆笔触 SDF 对齐]]"
  - "[[日志/2026-08-24-FullPaint移动端比例与可见阈值对齐|Full Paint 移动端比例与可见阈值对齐]]"
  - "[[日志/2026-08-24-Poem恢复hK四瓦片纹理契约|Poem 恢复 hK 四瓦片纹理契约]]"
  - "[[日志/2026-08-24-源码树叶全局粒子层与位置Pass重建|源码树叶全局粒子层与位置 Pass 重建]]"
  - "[[日志/2026-08-24-树叶交互边界与WebGL上下文降级|树叶交互边界与 WebGL 上下文降级]]"
  - "[[日志/2026-08-24-流体Stencil批处理与四帧活跃历史重建|流体 stencil 批处理与四帧活跃历史重建]]"
  - "[[日志/2026-08-24-烘焙相机入口与指针视差对齐|烘焙相机入口与指针视差对齐]]"
  - "[[日志/2026-08-24-流体AdvectionV2多步轨迹与交付QA|流体 Advection v2 多步轨迹与交付 QA]]"
  - "[[日志/2026-08-24-源码Reveal双时序Profile与交付QA|源码 Reveal 双时序 Profile 与交付 QA]]"
  - "[[日志/2026-08-24-源码Reveal中间态视觉证据与原站基线限制|源码 Reveal 中间态视觉证据与原站基线限制]]"
  - "[[日志/2026-08-24-FullPaint双层视频回退与兼容性QA|Full Paint 双层视频回退与兼容性 QA]]"
  - "[[日志/2026-08-25-Cutouts独立SDF阴影层重建与浏览器QA|Cutouts 独立 SDF 阴影层重建与浏览器 QA]]"
  - "[[日志/2026-08-25-全局Ground背景与阴影职责重建及交付QA|全局 Ground、Background 与阴影职责重建及交付 QA]]"
  - "[[日志/2026-08-25-Ground实例批次与Reveal状态验证|Ground 实例批次与 Reveal 状态验证]]"
  - "[[日志/2026-08-25-自定义光标圆形指示器修复与QA|自定义光标圆形指示器修复与 QA]]"
  - "[[日志/2026-08-25-五视口交付回归与光标状态门禁修正|五视口交付回归与光标状态门禁修正]]"
  - "[[日志/2026-08-25-主工程Definition边界迁移与空闲悬停流体修正|主工程 Definition 边界迁移与空闲悬停流体修正]]"
  - "[[日志/2026-08-25-主工程运行时Reducer边界迁移与五视口QA|主工程运行时 Reducer 边界迁移与五视口 QA]]"
  - "[[日志/2026-08-25-主工程内容Definition与DOM适配|主工程内容 Definition 与 DOM 适配]]"
  - "[[日志/2026-08-25-主工程World资源定义迁移与QA|主工程 World 资源定义迁移与 QA]]"
  - "[[日志/2026-08-25-主工程Runtime输入选择音频契约边界迁移与QA|主工程 Runtime 输入、选择、音频与契约边界迁移]]"
  - "[[日志/2026-08-25-主工程Paper职责子契约解构与QA|主工程 Paper 职责子契约解构与 QA]]"
  - "[[日志/2026-08-25-主工程Paper场景呈现契约与FullPaint映射QA|主工程 Paper 场景呈现契约与 Full Paint 映射 QA]]"
  - "[[日志/2026-08-25-主工程Paper身份与Reveal契约收窄及QA|主工程 Paper 身份与 Reveal 契约收窄及 QA]]"
  - "[[日志/2026-08-25-主工程设备能力与移动端降级Definition边界及空闲笔刷QA|主工程设备能力与移动端降级 Definition 边界及空闲笔刷 QA]]"
  - "[[日志/2026-08-25-主工程RuntimeReducer去除历史默认配置与QA|主工程 Runtime Reducer 去除历史默认配置与 QA]]"
  - "[[日志/2026-08-25-源码Paper分组Reveal延迟与层同步QA|源码 Paper 分组 Reveal 延迟与层同步 QA]]"
  - "[[日志/2026-08-25-Paper世界变换提取与五视口QA|Paper 世界变换提取与五视口 QA]]"
  - "[[日志/2026-08-25-Paper隐藏时间线与RestartQA|Paper 隐藏时间线与 Restart QA]]"
  - "[[日志/2026-08-25-流体笔刷源参数与可见波纹幅度收敛QA|流体笔刷源参数与可见波纹幅度收敛 QA]]"
  - "[[日志/2026-08-25-Paper与FullPaint共享Reveal噪声边界及Shader学术对照QA|Paper 与 Full Paint 共享 Reveal 噪声边界及 Shader 学术对照 QA]]"
supersedes: null
---

# 当前待办

## 目标优先级

### A. 水彩 WebGL 底层还原

- 继续以可观测行为为依据完善 26 个纸片的实例化几何、SDF 裁切、纸张弯曲、LUT、透明度和 reveal 中间态。
- 继续校准流体模拟的 external force、advection、pressure、accumulation、笔刷速度、前后轨迹、帧率无关衰减和纸片/地面 UV 映射。
- 完善 Ground、草叶、树叶、阴影投影、相机时间线、场景标题和六组 Full Paint 的联动。
- 已将树叶从每纸 30 点简化漂移提升为源码式全局 1024-instance + 32×32 position pass，并接入射线命中/移动注入；仍需 CDP 验证 shader 实编译、慢速/快速移动、寿命衰减、fog 与移动端实际降级。
- 已按源码收窄树叶交互边界：只接受 `paintIntersection` 的纸片命中并过滤 `uv2.x <= 0`，Ground 命中不再误注入；仍需 CDP 验证纸片边缘与颜色渐变。
- 已恢复 Fluid batch pass 的 stencil 结构：七个 simulation target 开启 stencil，新增独立 stencil mesh、`aStencilActive` 和 `wasActive`→`wasActive4` 四帧历史；普通 pass 只清 stencil 以保留其他 atlas tile，真实五视口 GPU QA 已通过结构、编译与 Full Paint 像素读数，仍需专门的跨 tile pigment 差分验证。
- 已对齐 baked camera 的源码入口与指针参数：`Camera_Action_Baked` 59.7667 秒、5 秒 `sine.out` 入口、NDC `2.55°/9.2°` 两轴和 `0.3` 指数阻尼，移动端/reduced-motion 禁止视差；真实五视口 QA 已验证相机时长、入口契约、移动端视差开关和滚动推进，仍需专门的指针移动截图确认 world-space 视差下纸片、射线和阴影的共同投影。
- 已恢复源码 active advection v2 的多步 backtrace/forward-trace 链，并在 atlas region 内夹取采样；真实五视口 GPU QA 已确认 shader/FBO、Full Paint、ellipse force、Stencil、相机、Poem、音频和降级链路通过，仍需下一轮做原始站点与 Branch 的截图/像素差分。
- 已将纸片 reveal 拆为可复现的 `delivery` 与 `source` profile：默认交付轨道保持 3/5/7 秒和普通纸片 edge catch-up，`?reveal=source` 恢复源码 7/10/15 秒并关闭 Branch 补全层；五视口 QA 已同时通过两套时序，并新增固定 `timeline=1.5s / uRevealProgress=1.5` 的源码中间态截图；原站实时页面受 hCaptcha 初始化阻断，后续以本地原始提取和 Branch 证据做差分。
- 已补齐原站独立 Full Paint simulation instance：26 个纸片 region 之外增加第 27 个固定分辨率 region，并让全屏笔刷与局部纸片状态隔离；仍需通过 CDP 视觉回归确认中间态。
- 已为第 27 个 Full Paint region 增加进入时的局部 scissor reset，退出再进入不会继承上一轮全屏 pigment，也不会清空 26 张纸片；仍需通过浏览器 QA 确认实际像素读数。
- 已为 GrassLayer 恢复源码 patch 级 bounding box/sphere，保留 frustum culling 的同时避免滚动镜头按 0.1×0.1 blade 原型错误剔除草簇；仍需通过桌面滚动截图确认可见连续性。
- 已按源码从 GLB 世界四元数恢复纸片实例 yaw，修复 `tree_1`、`viaduc_1` 等节点自身朝向被 `layers` 父节点覆盖的问题；已恢复 `revealType: fade` 的平面 3 秒 alpha 淡入分支；真实五视口已确认 Paper yaw 与 GLB world position 对齐，仍需通过固定中间态截图/像素差分确认实际视觉结果。
- 已恢复 Full Paint 的源码 `uScale` 展开/收拢、reveal global intensity alpha、退出期间继续合成和 resize reveal 点更新；仍需通过浏览器截图确认全屏墨迹边缘与视频层过渡。
- 已恢复 Ground 的源码 0→1/0.4 秒 alpha 生命周期，并建立 CSS 布局尺寸与 renderer drawing-buffer 尺寸的分离契约，覆盖纸片、地面、草、阴影、标题、诗歌和 Full Paint；仍需通过 DPR>1 与横竖屏 CDP 证据确认屏幕空间对齐。
- 已在 per-paper Ground 之外恢复源码全局 `SourceGlobalGroundLayer`：2000×2000 plane、GLB Ground world transform、`renderOrder=-3`、`receiveShadow`、`uShadowMap/uShadowStrength=0.9`、共享 Lighting/Background、fog 与 drawing-buffer resolution；最终五视口 QA 已通过，下一步仍是同 seed 像素差分、Ground fog/亮度/阴影边缘及 DPR>1 视觉收敛。
- 已将纸片 Ground 从逐纸渲染 Mesh 收敛为源码式单个 `SourceGroundsBatch` InstancedMesh：26 个实例槽、`instanceId` 射线回写、`uVisible[]/uAlpha[]/uAtlasRemap[]` 和各实例 simulation remap；统一 `freeze=6` 五视口回归已通过且默认水彩材质参数未改。
- 已建立 Branch 版 `ExperienceDefinition` 解构边界：资源、六场景视频、五个音频、26 个纸片、Ground atlas、reveal/camera timing 和设备能力由同一对象注入入口、水彩世界、流体和 Full Paint；未替换已验证的 shader 参数。用户反馈的空闲 hover 无限扩散已修正为仅由最近真实绘画输入/按压刷新 activity grace，最终五视口实时 QA 确认所有 case `idleCalls=0`。
- 已按主工程 `ExperienceRuntimeState`/reducer 解构 Branch 运行时边界：资源进度与就绪、boot/loading/exploring/landscape/tail/error、诗句/场景派生、静音、性能档位和失败信息均由纯 reducer 维护；Full Paint SHOW/HIDE 映射为 `landscapeScene`。五视口 QA 与 9333 CDP 探针确认 runtime、Full Paint、滚动派生和错误链路通过。
- 已按主工程 `copy`/`fonts`/`tail` 内容边界解构 Branch：诗句 sourceMarkup、加载/交互文案、尾部权益、完整 FAQ、静态 token 和字体 family 由 definition 注入 DOM/TextCanvas/AudioManager/标题层；最终五视口 QA 通过，控制台错误与远程资源均为 0。
- 已按主工程 `worldAssets` 边界解构 Branch：模型、纸张/图集/噪声/草叶/树叶/Poem/MSDF、LUT、Ground KTX2 与 Basis transcoder 由 `app/src/content/world.ts` 提供，静态资源清单和 KTX2 setup 均从 `ExperienceDefinition.world.assets` 读取；最终五视口 QA 已确认资源绑定和 WebGL 行为通过。
- 已按主工程 `src/content/scenes.ts` 解构六场景 metadata：Branch `ExperienceDefinition.scenes` 现在统一提供 label、英文 title 和 `focusProgress`，runtime scene starts 从同一进度值派生并由五视口 QA 逐项断言；仍需继续审计 paper manifest 的其他 source-derived 字段。
- 已继续收敛主工程 World 内容边界：新增 `app/src/content/atlas.ts` 承载 26 个 SDF/纹理 remap 与 layer schedule，`WatercolorView` 不再直接读取 atlas JSON；同时将主工程已注册的 `rgbaNoise` 加入实际静态资源清单，五视口 QA 已通过，下一步继续拆 paper 的 ground/sdf/leaves/cutout 字段。
- 已将 `PAPERS_CONFIG` 的运行时读取统一收敛到 `app/src/content/papers.ts` 与 `ExperienceDefinition.world.papers`：ground、SDF、leaves、shadow/hole、fade、title、reveal timing 的 26 项 source-derived manifest 已由 Definition 注入，五视口 QA 已验证结构与 WebGL 行为，下一步可继续按职责拆分更细子定义。
- 已继续审计主工程 runtime：将当前 Branch 实际使用的滚动时间线输入、诗句/场景选择、Definition-derived runtime contract 和音频主题语义迁入 `app/src/experience/runtime/`；未迁入主工程未挂载的 React 音频 Hook，避免第二套播放器。`npx tsc --noEmit`、build、73/73 完整性、五视口/source reveal QA 均通过。
- 已继续拆分 Paper 子职责：新增 `app/src/content/paper-layers.ts`，从只读 `paperManifest` 派生 `world.paperLayers.ground/sdf/vegetation/shadow` 四组 26 项窄契约；LeavesLayer、WatercolorView 的 Ground、SDF/reveal、shadow/cutout 路径分别消费对应契约，完整 `world.papers` 仍保留给中央编排和兼容层。五视口/source reveal QA 通过；下一步做固定 seed/freeze/reveal 中间态的主工程与 Branch 视觉差分，不因末态纹理密度改变当前材质或波纹参数。
- 已继续收窄 Paper 场景呈现边界：`world.paperLayers.presentation` 承载 26 项 Paper identity、title、CTA 和 sceneIndex；PaintingTitles 与 FluidSimulation 的 scene→主纸选择改用该契约，不再直接读取完整 `world.papers`。五视口/source reveal、标题交互、Full Paint、流体与降级 QA 通过；下一步继续审计中央 Paper identity/reveal/GLB transform 边界，并做固定中间态主工程/Branch 视觉差分。
- 已继续收窄 Paper identity/reveal 边界：导出 `PaperIdentityContract`，WatercolorView 的 PaperEntry 与 PaperInstanceConfig 改为携带 identity；普通 reveal 读取 identity.startAt，fade/default 读取 SDF 契约，Ground 几何读取 Ground 契约。五视口/source reveal、纸片 yaw、标题、27 个 simulation region、流体与降级 QA 通过；下一步继续审计 GLB transform/reveal state 生命周期并做固定中间态视觉差分。
- 已按原始 `papersContainer.show()` 恢复 Paper 分组 reveal 调度：`startAt` 负责建立动画，首纸 1.5 秒入口延迟（`?noIntro` 可跳过），同组按 manifest 顺序每项错峰 0.3 秒；Paper、Cutout、Ground 共用该延迟，避免把层动画错误地加到滚动触发阈值。GLB Paper authoring transform 已改为 `getWorldPosition/getWorldQuaternion/getWorldScale` 证据，可见实例仍使用 geometry-derived meshSize；本轮又恢复 Paper/Cutout/Shadow/Ground 共享 0.5 秒 hide timeline，隐藏期间禁止重复 reveal，完成回调后才清理 `revealed`/Ground visible。五视口 QA 已通过，下一步继续固定中间态截图/像素差分。
- 已继续统一设备能力与移动端降级：`ExperienceDefinition.assets.device/videoResources` 成为 Cursor、Grass、Leaves、ScrollCamera、TextCanvas、UIView、PaintManager、FullPaint 的唯一来源，设备敏感 Watercolor 层延迟到 init；移动端五视口验证草/树叶关闭、0.6 秒长按、mobile 视频和 viewport-ratio Full Paint，桌面验证完整 vegetation/0.3 秒长按。`config/assets.ts` 仅保留历史映射，无 runtime import。
- Auto 服务器的末态截图不能直接作为手动中途显现画面的视觉基线；继续比较前必须固定 `freeze`、`reveal`、视口、等待时长和指针状态，暂不因末态纹理变密调整默认 delivery 参数。
  - 已针对用户反馈的“实际场景整棵树高频闪烁、波纹范围太大且过强”完成一轮源证据收敛：PaintManager 使用原始 `cursorSize / screenSpaceSize` 推导椭圆笔刷，external force 使用未乘额外 60Hz 补偿的原始 NDC 速度，FluidSimulation 使用源码椭圆 SDF，Paper simulation UV 只做源码 Y 翻转；Paper 可见层额外使用 `DELIVERY_SIMULATION_VISUAL_GAIN = 0.015` 压低交付湿润响应，保留 simulation 诊断数据不变。固定轨迹与 5 秒衰减 QA 已通过，画面从整树高频斜纹收敛为局部低幅湿润变化；后续必须用固定主工程/Branch 中间态证据继续确认该增益，再做像素差分，不要凭末态截图扩大波纹参数。
- 已按原始提取修正 Paper 与 Full Paint 共享 `noise/rgb-generated` 的 wrap 契约：base 灰度噪声为 `RepeatWrapping=1000`，final/generated RGB 噪声为 `MirroredRepeatWrapping=1002`，并在 QA 中同时断言两个消费者，避免 Full Paint 初始化覆盖 Paper。五视口、source reveal、shader、交付降级和上下文恢复 QA 已通过；Curtis 计算水彩分层与 Stam 稳定流体资料已纳入后续 Shader 推理辅证，仍以主工程/原始提取事实为准。
- 已修复桌面自定义光标的四角/断裂观感：恢复 95px 完整 SVG 圆环、中心点、SVG overflow 和画纸悬停圆形笔刷态；五视口 QA 已覆盖默认态与 0.5× 悬停态，未改变水彩材质与 Reveal 参数。
- 已按源码拆分 Background、ShadowProjection 与 Cutouts 的职责：全屏背景只合成渐变、屏幕高光和三态 fog，Ground/Paper/CutoutShadowLayer 分别消费投影阴影；已恢复 24 个带孔纸片的 SDF cutout shadow 和 0.4 秒 alpha 生命周期，真实五视口 QA 已通过；仍需通过同 seed 截图/像素差分确认 fog depth、基础亮度、Ground 组织和阴影边界没有视觉漂移。
- 已把原始 27/28 Background fullscreen shader 从 ShadowProjection 内联近似提取到 `app/src/shaders/background.ts`，并让 composite 复用 Paper 的 `uBackground/uLighting` 与显式 `uRatio`；第二次完整五视口 QA、源码 reveal、失败降级、context loss 与 9333 单页面 reload 通过。后续继续做固定中间态/主工程像素差分，不因本轮结构收敛扩大波纹或阴影参数。
- 已把 renderer、KTX2 与 DOM 初始化纳入同一异常边界，初始化失败会释放 renderer 并进入可读 fallback；同时恢复 Divergence 的单格差分与实例 `dt` 归一化，仍需通过 CDP 确认低速笔触和 pressure 中间态。
- 已进一步恢复原始实例 `dt` 契约：每个 simulation tile 使用 `0.008 × tileWidth / atlasWidth`，五视口 QA 会检查所有实例的缩放结果；仍需通过 CDP 确认不同纸片分辨率下的 advection/pressure 视觉一致性。
- 对每项底层修改补充中间态、低速、桌面、移动和 reduced-motion 证据，避免只验证最终截图。

### B. 当前页面交付与兼容性

- 恢复并验证双阶段加载、Enter 解锁、声音主题切换、Back、Restart、诗句全屏和 Full Paint 的完整生命周期。
- 根据最终产品范围逐步补齐页面尾部内容、FAQ、静态 Offers、分享/奖项展示和无障碍语义，同时保持不接入远程商业服务。
- 继续验证桌面/移动布局、触摸长按、滚动阻尼、WebGL 不可用、reduced-motion、资源加载失败和页面可读性降级。
- 已修复 Branch manifest 中 4 个资源的字节/哈希不一致；`npm run verify:integrity` 已恢复为 73/73 通过，继续把完整性检查纳入每轮底层渲染回归。
- 已恢复尾部静态可交付内容：权益、静态 offer、5 条 FAQ、分享/奖项文案与 Restart；FAQ 支持键盘、`aria-expanded`、无 IntersectionObserver 降级和 reduced-motion 路径。
- 已补齐资源失败与初始化异常 fallback：静态资源缺失不会继续构建空 WebGL 场景，页面会显示可读说明并保留下方文字内容。
- 已补齐 WebGL context loss 降级：停止渲染 ticker、解绑失效运行时、静音音频、阻止延迟 boot/resize 继续提交，并在上下文恢复时保持可读 fallback；仍需通过 CDP 的 `WEBGL_lose_context` 真实验证事件链。
- 已补齐 Full Paint 懒加载视频的媒体错误边界：base 失败会停止全屏合成、释放 VideoTexture、恢复主场景音频并显示可读提示；over 失败按源码可选纹理契约回退到 base 并保持渲染/交互，真实五视口 QA 已验证两条路径，仍需通过同 seed 截图确认视频裁切和墨迹边缘。
- 已将 FAQ、Restart 和静态权益绑定移到 WebGL 分支之前，并把 reduced-motion QA 提升为实际检查 FAQ 可见性与 Restart 回顶；仍需通过真实 CDP 验证浏览器事件链和 renderer 构造失败路径。
- 已补充流体 pass 的结构验收：五视口 QA 会检查 Divergence 使用单格邻域和 `vDt` 归一化；在真实 CDP 建立前仍不能替代 shader 编译与实际像素证据。
- 已补充所有 simulation tile 的实例 `dt` 缩放断言，覆盖 26 张纸片和第 27 个 Full Paint tile；不同 atlas 分辨率下的实际 advection、pressure 和 pigment 仍需 CDP 像素证据。
- 已恢复实例 active/wasActive 生命周期：普通命中 5 秒 grace、离开后可失活，Full Paint 只激活第 27 个 tile；QA 已加入状态级断言，实际衰减与切换像素仍需 CDP。
- 已将 external-force 正式路径从 Branch 误用的 `sdUnevenCapsule` 改回源码实际启用的 v2 `sdEllipse`，并恢复目标 tile 的 `wasActive` 清零；QA 已加入 shader 合约断言，仍需通过 CDP 验证宽/窄纸片、低速、按下和快速移动下的真实笔触边缘与 pigment 输出。
- 已按源码修正 Full Paint 第 27 个 tile 的横竖屏比例：竖屏固定高度 512、横屏固定宽度 1024；同时恢复 `visibleProgress > 0.8` 的 Full Paint 可见阈值、退出期间 active 抑制和原始 fragment 输出语义；仍需通过 CDP 验证视频边缘与移动端像素裁切。
- 已把 Poem 从旧的整屏四边形改为原始职责边界：独立 UI 相机下的 Full Screen simulation 纸张背景 + TextMesh；同时恢复 `TextCanvas` 的 `hK` 固定诗句、400×2304 尺寸、四瓦片 UV 与 `poem/texture` 资源契约，主滚动页继续保留 DOM 三瓦片路径；仍需通过 CDP 验证真实诗句像素、滚动位置、横竖屏布局和开启/关闭中间帧。

### C. 工程维护

- 保持 Branch 的 `app/`、`sources/`、`evidence/`、`archive/` 和 `wiki_memory/` 独立，不引用主项目路径或运行时。
- 继续使用独立的 TypeScript、build、integrity、CDP smoke 和五视口 QA 记录每次复刻结果。

- 继续以本地原始提取镜像和已接受 QA 作为视觉、交互与资源完整性基线。
- 用户明确验收新的视觉精修候选后，再决定是否把候选证据迁入 `evidence/qa/`。
- 后续实质开发按本项目日志规则记录，不回写 Verminoble 主线记忆。
- 本轮重跑后的完整五视口 QA 已通过；滚动 100px 门禁已改为按当前内容高度、travel multiplier 和 camera tail 公式计算，避免固定旧阈值误报。
- `config/assets.ts` 暂作为历史配置保留；本轮已确认无模块直接读取它，后续仍不删除，避免切断原始事实映射。
- `runtime/reducer.ts` 已移除无外部 config fallback；正常入口必须使用 Definition-derived `runtimeContract`，避免历史 scene starts/poem breakpoints 形成第二套运行时事实。
