---
type: state
status: active
kind: architecture
importance: high
updated: 2026-08-25
topic: project-overview
source_logs:
  - "[[日志/2026-08-20-工程记忆初始化|工程记忆初始化]]"
  - "[[日志/2026-08-24-主线与Branch复刻架构差距比较|主线与 Branch 复刻架构差距比较]]"
  - "[[日志/2026-08-24-纸片世界朝向与Fade显现对齐|纸片世界朝向与 Fade 显现对齐]]"
  - "[[日志/2026-08-24-FullPaint显现与生命周期对齐|Full Paint 显现与生命周期对齐]]"
  - "[[日志/2026-08-24-Ground与渲染分辨率契约对齐|Ground 与渲染分辨率契约对齐]]"
  - "[[日志/2026-08-24-背景合成与阴影职责对齐|背景合成与阴影职责对齐]]"
  - "[[日志/2026-08-24-运行时初始化与流体散度对齐|运行时初始化与流体散度对齐]]"
  - "[[日志/2026-08-24-FullPaint移动端比例与可见阈值对齐|Full Paint 移动端比例与可见阈值对齐]]"
  - "[[日志/2026-08-24-Poem视图TextMesh与水彩背景重建|Poem 视图 TextMesh 与水彩背景重建]]"
  - "[[日志/2026-08-24-Poem恢复hK四瓦片纹理契约|Poem 恢复 hK 四瓦片纹理契约]]"
  - "[[日志/2026-08-24-流体椭圆笔触SDF对齐|流体椭圆笔触 SDF 对齐]]"
  - "[[日志/2026-08-24-源码树叶全局粒子层与位置Pass重建|源码树叶全局粒子层与位置 Pass 重建]]"
  - "[[日志/2026-08-24-树叶交互边界与WebGL上下文降级|树叶交互边界与 WebGL 上下文降级]]"
  - "[[日志/2026-08-24-流体Stencil批处理与四帧活跃历史重建|流体 stencil 批处理与四帧活跃历史重建]]"
  - "[[日志/2026-08-24-烘焙相机入口与指针视差对齐|烘焙相机入口与指针视差对齐]]"
  - "[[日志/2026-08-24-流体AdvectionV2多步轨迹与交付QA|流体 Advection v2 多步轨迹与交付 QA]]"
  - "[[日志/2026-08-24-源码Reveal双时序Profile与交付QA|源码 Reveal 双时序 Profile 与交付 QA]]"
  - "[[日志/2026-08-24-源码Reveal中间态视觉证据与原站基线限制|源码 Reveal 中间态视觉证据与原站基线限制]]"
  - "[[日志/2026-08-24-FullPaint双层视频回退与兼容性QA|Full Paint 双层视频回退与兼容性 QA]]"
  - "[[日志/2026-08-25-Cutouts独立SDF阴影层重建与浏览器QA|Cutouts 独立 SDF 阴影层重建与浏览器 QA]]"
  - "[[日志/2026-08-25-全局Ground背景与阴影职责重建及交付QA|全局 Ground、Background 与阴影职责重建及交付 QA]]"
  - "[[日志/2026-08-25-Ground实例批次与Reveal状态验证|Ground 实例批次与 Reveal 状态验证]]"
  - "[[日志/2026-08-25-自定义光标圆形指示器修复与QA|自定义光标圆形指示器修复与 QA]]"
  - "[[日志/2026-08-25-五视口交付回归与光标状态门禁修正|五视口交付回归与光标状态门禁修正]]"
  - "[[日志/2026-08-25-主工程Definition边界迁移与空闲悬停流体修正|主工程 Definition 边界迁移与空闲悬停流体修正]]"
  - "[[日志/2026-08-25-主工程运行时Reducer边界迁移与五视口QA|主工程运行时 Reducer 边界迁移与五视口 QA]]"
  - "[[日志/2026-08-25-主工程内容Definition与DOM适配|主工程内容 Definition 与 DOM 适配]]"
  - "[[日志/2026-08-25-主工程World资源定义迁移与QA|主工程 World 资源定义迁移与 QA]]"
  - "[[日志/2026-08-25-主工程Runtime输入选择音频契约边界迁移与QA|主工程 Runtime 输入、选择、音频与契约边界迁移]]"
  - "[[日志/2026-08-25-主工程Paper职责子契约解构与QA|主工程 Paper 职责子契约解构与 QA]]"
  - "[[日志/2026-08-25-主工程Paper场景呈现契约与FullPaint映射QA|主工程 Paper 场景呈现契约与 Full Paint 映射 QA]]"
  - "[[日志/2026-08-25-主工程Paper身份与Reveal契约收窄及QA|主工程 Paper 身份与 Reveal 契约收窄及 QA]]"
  - "[[日志/2026-08-25-主工程设备能力与移动端降级Definition边界及空闲笔刷QA|主工程设备能力与移动端降级 Definition 边界及空闲笔刷 QA]]"
  - "[[日志/2026-08-25-主工程RuntimeReducer去除历史默认配置与QA|主工程 Runtime Reducer 去除历史默认配置与 QA]]"
supersedes: null
---

# 项目概览

David Whyte Experience 是一个仅本地使用的高保真水彩体验页重建工程。它不发布、不部署、不接入账户、购买、预约或远程商业服务。

## 已确认的长期目标

继续以 Branch 的独立工程为复刻推演主线，优先提高水彩 WebGL 底层重建深度：纸片材质、流体笔触、reveal 时间线、Ground、草、阴影、相机、射线和 Full Paint 都要逐项接近可观测行为；同时持续提高当前可交付页面的加载、诗句、音频、响应式、reduced-motion、尾部内容、FAQ、Restart 和资源降级兼容性。

底层视觉还原与页面交付兼容性是并行目标。Branch 的推演实现不直接等同于主项目真实架构，后续仍保持 Branch 自己的源码、资产、依赖、证据和记忆边界。

## 入口

- 应用：`../../app/`
- 原始基线：`../../sources/original-extraction/`（只读）
- 当前验收证据：`../../evidence/qa/accepted-2026-08-20/`

## 常用命令

```powershell
Set-Location .\app
npm ci
npm run dev
npm run verify
npm run qa
```

完整性检查默认验证 73 个本地素材与内部原始镜像的一致性。参见
[[知识/规范/来源与完整性|来源与完整性]]、[[知识/流程/验收基线|验收基线]]。

2026-08-24 已将流体图集对齐到原站的 26 张纸片 + 1 个独立 Full Paint 实例；后者使用独立 remap 和固定分辨率 tile，不再复用被选纸片的历史笔触。真实 Chrome CDP 五视口 QA 已通过 shader/FBO 与 Full Paint 生命周期验证；新的 27-region 与原始站点之间的截图/像素差分仍待后续视觉收敛。

同日又补齐了 Full Paint tile 的局部清空、静态尾部的权益与 FAQ 内容，以及资源失败/初始化异常的文字 fallback。尾部交互不依赖远程购买、账户或分享服务；缺少浏览器 CDP 时只能确认代码和构建证据，不能把这些行为宣称为已完成视觉验收。

本轮又按原始源码补齐了纸片 GLB 世界 yaw 到实例矩阵的传递，并恢复透明背景纸片的 `fade` 显现分支；静态验证已通过，运行时五视口与中间态证据仍待 CDP 条件具备后补齐。

本轮继续深入 Full Paint：恢复源码的缩放与 reveal alpha 语义、退出期间的渲染生命周期和 resize reveal 点更新；静态验证已通过，实际全屏水彩中间态仍待 CDP 证据。

本轮继续深入 Ground 与屏幕空间兼容性：恢复 Ground 首次 reveal 的 0→1 alpha 生命周期，并将 CSS 布局尺寸与 renderer drawing-buffer 尺寸分离，统一到纸片、地面、草、阴影、标题、诗歌和 Full Paint 的 `uResolution`/RenderTarget；静态验证已通过，DPR>1 与横竖屏视觉证据仍待 CDP。

本轮继续深入 Background 与 ShadowProjection：按源码拆开全屏背景与投影阴影职责，恢复主相机 view/far-clip 矩阵参与 fog depth/world position，并让重启同步清空阴影 alpha GPU attribute；静态验证已通过，fog 中间态、基础亮度和阴影边界仍待 CDP。

本轮继续深入 Ground：按源码把逐纸 Ground Mesh 收敛为覆盖 26 个纸片索引的单个实例批次，并通过 `instanceId` 恢复命中索引；固定 `freeze=6` 的 smoke 已通过。手动页面与 Auto 服务器的视觉差异确认来自 reveal 时间状态，默认纸片材质和波纹参数保持不变。

本轮修复 Branch 自定义光标与原站完整圆环的差距：补齐 SVG 圆环的无断线绘制、溢出显示、中心点和画纸悬停态，并把桌面/移动端光标契约加入 QA；Chrome 定点验证已确认 95px 完整圆环，未改动水彩渲染参数。

本轮在修正光标 QA 对源码悬停缩放的误判后，统一 `freeze=6` 完成 1440×900、1920×1080、2560×1440、390×844、430×932 五视口回归；27 个 simulation region、底层层次、Poem、Full Paint、音频、降级、FAQ、Restart 和光标均通过，console errors/remote resources 为 0。该结果仍是 Branch 自证，不替代主工程解构和同 seed 像素差分。

本轮继续深入运行时边界与流体压力链：把 renderer/KTX2/DOM 初始化纳入 fallback 捕获，并按原始源码恢复 Divergence 单格差分和 `dt` 归一化；静态验证需以本轮最终命令为准，低速笔触、pressure、初始化失败页面和五视口兼容性仍待真实 CDP。

本轮继续深入流体实例契约：恢复原始 `0.008 × tileWidth / atlasWidth` 的 per-tile `dt`，并将 26 张纸片与独立 Full Paint region 纳入一致性断言；实际不同分辨率 tile 的视觉对比仍待真实 CDP。

本轮继续提高交付降级：FAQ、Restart 与静态权益绑定不再依赖 WebGL renderer，reduced-motion fallback 的 QA 已加入答案可见性与回顶行为断言；真实浏览器结果仍待 CDP。

本轮继续深入流体实例生命周期：按原始 active getter 恢复普通纸片 5 秒延迟失活和 Full Paint 第 27 个 tile 的独立 active 状态，并加入 Restart/切换前的清理契约；实际视觉衰减和过渡仍待 CDP。

本轮继续深入水彩底层笔触：确认原始 external-force 的 capsule 只是 v1 注释实验，正式路径是 ratio-corrected ellipse v2；Branch 已改用椭圆 SDF、恢复 `wasActive` 清零，并保留 tile 横纵半径的屏幕空间适配。shader 编译、快速移动连续性和真实 pigment 边缘仍待 CDP。

本轮继续深入 Full Paint 的移动端和输出契约：按源码恢复竖屏固定高度 512 的第 27 个 tile、`visibleProgress > 0.8` 的可见阈值以及原始 fragment 直接输出语义；静态验证已通过，移动端视频裁切、进入/退出边界和像素级 active 抑制仍待 CDP。

本轮继续深入 Poem 架构：按原始 `AK`/`Q5` 契约恢复 `hK` 完整诗句数据、400×2304 排版、四瓦片 UV 与 `poem/texture` 纹理，同时保留独立 UI 相机、Full Screen simulation 纸张背景和淡出期间持续渲染；构建与静态 QA 契约已通过，真实诗句像素、滚动定位和横竖屏转场仍待 CDP。

本轮继续深入树叶底层：将每纸 30 点的简化漂移替换为源码式全局 1024-instance、32×32 position pass、命中点实例矩阵和 0.02 秒注入队列，并把桌面/移动端降级与 `leavesState` QA 契约接入；TypeScript、构建和资源完整性已通过，树叶 shader 实编译、交互生命周期与像素效果仍待 CDP。

本轮继续收窄树叶交互边界并提高运行时兼容性：树叶只接收源码纸片 `paintIntersection` 且过滤 `uv2.x <= 0`，Ground 不再误注入；WebGL context loss 会停止 ticker、静音音频、阻止 boot/resize 延迟提交并保持可读 fallback，恢复事件要求重新加载重建资源图。静态验证已通过，真实事件链和视觉结果仍待 CDP。

2026-08-24 本轮继续深入 Fluid batch pass：七个 simulation target 开启 stencil，加入独立 stencil mesh、`aStencilActive` 与 `wasActive`→`wasActive4` 四帧活跃历史；普通 pass 只清 stencil，避免处理单张纸片时擦除其他 atlas tile，并在 hidden→visible 后强制两帧同步。TypeScript、构建与资源完整性已通过；真实 GPU stencil、跨 tile pigment 保留、离开冲刷帧与 hidden/visible 兼容性仍待 CDP。

2026-08-24 本轮继续深入 baked camera：恢复源码 5 秒 `sine.out` 入口、NDC `2.55°/9.2°` 指针轴向与 `0.3` 阻尼，保留 Branch 的 GLB world transform 采样，并将父容器视差应用到位置与朝向；真实镜头中间帧、射线/阴影共同投影仍待 CDP。

2026-08-24 本轮继续深入流体 advection：恢复原始 active v2 的多步 backtrace/forward-trace 链，并把 atlas 采样限制在当前纸片区域；同步修正移动端相机运行时检测、fade 首帧状态和 QA 的真实源码契约。真实 Chrome CDP 五视口 QA 已通过，报告 `passed: true`、无 console errors 与 remote resources；仍保留原始站点与 Branch 的截图/像素差分作为下一阶段视觉收敛工作。

本轮又把纸片 reveal 明确拆成 `delivery` 与 `source` 两个可复现 profile：默认继续使用用户选定的 3/5/7 秒和普通纸片边缘追赶；URL 加 `?reveal=source` 时恢复源码 7/10/15 秒，并关闭 Branch 额外完整层补全。两套 profile 共用同一套四点 reveal、shader、流体、Ground、Shadow 和相机契约；真实五视口 QA 同时通过默认交付轨道与源码时序检查，仍待原站/Branch 同 seed 截图像素差分决定未来默认时序。

本轮又为 `?reveal=source` 固定了 1440×900、timeline 1.5 秒的源码中间态截图和状态记录；远程原站试载受 `hcaptcha is not defined` 阻断，后续差分以本地原始提取和 Branch 自有证据为准。

本轮继续按原始 Full Paint 的可选第二纹理契约收窄资源失败边界：over 视频失败时回退到 base 并保持全屏绘画渲染，base 失败才退出到文字 fallback；真实 Chrome 五视口 QA 已验证两条路径、源码 reveal profile、Poem、Restart、reduced-motion、context loss 和 73/73 资源完整性。

2026-08-25 本轮进一步校正了阴影职责：原始 `uShadowMap` 消费者属于独立 `Cutouts` pass，不是 Paper 本体。Branch 已恢复 24 个带孔纸片的 InstancedBufferGeometry、SDF atlas 解码、切线空间深度偏移、cutout/map 双阴影混合和 0.4 秒 alpha 生命周期；真实 Chrome 五视口 QA 已通过，报告 `passed: true` 且无 console errors/remote resources。原始全局 Ground 与 Branch 当前的 per-paper Ground + fullscreen composite 拆分仍待后续像素级收敛。

同一轮继续向上游还原源码职责：Branch 新增独立 `SourceGlobalGroundLayer` 与 `globalGround` shader，恢复 GLB Ground 世界变换、2000×2000 平面、`renderOrder=-3`、`receiveShadow`、`uShadowMap/uShadowStrength`、共享 Lighting/Background、fog 和 drawing-buffer resolution；Background fullscreen composite、ShadowProjection、per-paper Ground 与 Cutouts 仍各自独立。最终 Chrome 五视口 QA 已通过，Global Ground、页面交付降级、资源完整性 73/73 和兼容性门禁均通过；同 seed 像素差分仍是下一步视觉收敛。

2026-08-25 本轮建立了 Branch 版 `ExperienceDefinition` 解构适配边界，将主工程的资源、场景、音频和 world 数据流映射到 Branch，同时修正空闲 hover 持续刷新流体活跃状态的问题。构建、73/73 资源完整性和现有页面实时探针通过；随后补跑的完整五视口 QA 也已通过。

同日又将根目录主工程 `ExperienceRuntimeState`/reducer 解构迁入 Branch：资源进度、资产就绪、runtime phase、滚动派生的诗句/场景、Full Paint landscape、静音、性能档位与错误态现在有纯状态边界，legacy visual phase 仍由现有 WebGL 转场维护。五视口 QA 重新通过，9333 单页面 CDP 探针确认 Full Paint→`landscapeScene=3`，没有新增 Chrome target。

2026-08-25 本轮继续把主工程的 `copy`、`fonts`、`tail` 内容层迁入 Branch definition：完整诗句 sourceMarkup、尾部 FAQ/offer/奖项文案和字体 family 由本地适配层应用到既有 DOM 壳，并被 Loader、TextCanvas、AudioManager、标题 CTA 消费。最终五视口 QA、source reveal、兼容性与完整性门禁通过，9333 保持单页面。

2026-08-25 本轮进一步解构主工程 `worldAssets`：Branch 新增本地 `app/src/content/world.ts`，统一模型、纹理、LUT、Ground KTX2、Poem/MSDF、Basis transcoder 与静态/视频资源清单；`Resources.setupKtx2` 和入口均消费 definition，不再由 loader 硬编码 transcoder 路径。最终五视口 QA 的 world binding、WebGL、资源完整性与兼容性门禁通过。

2026-08-25 本轮继续迁移主工程 `src/content/scenes.ts`：六个场景的 label、英文标题和 `focusProgress` 已进入 Branch `ExperienceDefinition.scenes`，兼容用的 camera time 从同一进度派生，runtime scene starts 不再从 `PAPERS_CONFIG` 隐式推断。五视口 QA、构建、73/73 完整性和 Chrome 单页面状态均通过；同 seed 像素差分仍是后续视觉收敛工作。

2026-08-25 本轮继续收敛主工程 World 内容边界：Branch 新增 `app/src/content/atlas.ts`，以 Definition 统一承载 26 个 SDF/纹理 remap 和 layer schedule，`WatercolorView` 不再直接读取 atlas JSON；同时补齐实际静态加载图中遗漏的 `rgbaNoise`。构建、73/73 完整性、五视口 WebGL QA 和 Chrome 单页面状态均通过；paper 的 ground/sdf/leaves/cutout 字段仍待后续按职责拆分。

2026-08-25 本轮继续拆解 Paper manifest：新增 `app/src/content/papers.ts`，让 26 项 ground、SDF、leaves、shadow/hole、fade、title 和 reveal timing 通过 `ExperienceDefinition.world.papers` 进入 WebGL runtime；shader、标题、Leaves 和 WatercolorView 不再直接绕过 Definition 读取 `config/papers.ts`。五视口 QA、构建、73/73 完整性和 Chrome 单页面状态通过，下一步继续做 source-derived 子职责拆分与固定中间态像素差分。

2026-08-25 本轮继续审计主工程 runtime：未直接搬入未挂载的 React 音频 Hook，而是将当前 Branch 真实运行路径所需的滚动时间线输入、诗句/场景选择、Definition-derived runtime contract 和音频主题语义抽出为纯边界。`ExperienceManager.runtimeContract` 由 Definition 派生，`ScrollController` 不再重复硬编码 travel multiplier；五视口、source reveal、流体、音频、降级与 73/73 完整性 QA 通过，9333 保持单网页。

2026-08-25 本轮继续解构 Paper 职责：根目录主工程当前可运行的 R3F scaffold 没有可直接复制的真实 Paper WebGL 底层，因此以 Branch 的只读 `paperManifest` 为事实来源，新增 `world.paperLayers`，派生 Ground、SDF、vegetation、shadow 四组窄契约。LeavesLayer、WatercolorView 的 Ground/reveal/cutout 路径分别消费对应子契约，中央编排仍保留完整 manifest 兼容层。五视口/source reveal QA 通过，9333 保持单网页；未调整波纹、材质、Reveal 或光标参数。

2026-08-25 本轮继续收窄 Paper 场景呈现边界：新增 `world.paperLayers.presentation`，承载 26 项 Paper identity、title、CTA 和 sceneIndex。`PaintingTitles` 不再直接读取 `paperManifest`，`FluidSimulation` 的 scene→主纸选择也改由该契约提供。五视口/source reveal、标题交互、Full Paint 27th region、流体生命周期和降级 QA 通过；9333 仍只有一个页面。

2026-08-25 本轮继续收窄 Paper runtime：`PaperIdentityContract` 统一 `index/name/startAt/sceneIndex`，WatercolorView 的 `PaperEntry` 与 `PaperInstanceConfig` 不再携带完整 `PaperConfig`；普通 reveal 使用 identity，fade/default 使用 SDF 契约，Ground 几何使用 Ground 契约。五视口/source reveal、27 个 simulation region、纸片 yaw、标题、流体和降级 QA 通过；9333 仍只有一个页面，未调整材质或波纹参数。

2026-08-25 本轮继续解构主工程设备能力边界：`ExperienceDefinition.assets.device/videoResources` 成为光标、草、树叶、相机、文字、UI、长按和 Full Paint 的统一来源，设备敏感 Watercolor 层延迟到 init 构造；同时停止真实指针输入后不再由平滑尾帧续期流体 activity。最终五视口、source Reveal、音频、reduced-motion、资源失败、Context Loss 和 73/73 完整性 QA 通过，9333 仍保持单页面；未调整水彩材质、波纹密度或 Reveal profile。

2026-08-25 本轮继续收敛主工程 Runtime 边界：删除 `runtime/reducer.ts` 内硬编码的 scene/poem 默认配置，`RuntimeReducerConfig` 只依赖窄的 `RuntimeSelectionConfig`，并要求生产调用链传入由 `ExperienceDefinition` 派生的合约；`ExperienceManager` 同时移除空 RuntimeContract 占位，改为 constructor 内真实初始化。五视口/source Reveal、资源完整性、降级与 context-loss QA 通过；9333 仍只有一个页面，未调整水彩材质、波纹密度或 Reveal profile。

## 当前状态入口

- [[当前状态/系统架构|系统架构]]
- [[当前状态/当前约束|当前约束]]
- [[当前状态/当前待办|当前待办]]
- [[当前状态/已知问题|已知问题]]
- [[决策/ADR-001-独立运行环境与主线隔离|ADR-001 独立运行环境与主线隔离]]
