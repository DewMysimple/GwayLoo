---
type: log
status: archived
kind: bug
importance: high
updated: 2026-08-20
topic: uv-alignment
source_logs: []
supersedes: null
---

# 2026-08-20｜UV 对齐修复

- 时间: 2026-08-20 23:51:00–23:58:10（北京时间）
- 对话类型: Bug处理
- 主题标签: UV映射、射线命中、流体、交互对齐
- 本轮目标: 修复鼠标位于纸片右侧时水彩却在左侧显现的水平镜像，并增加坐标回归检查。
- 日志索引: [[日志/MOC_工作日志|工作日志 MOC]]
- 相关知识: [[当前状态/系统架构|系统架构]]、[[知识/流程/验收基线|验收基线]]

## 已确认的决策

- 继续以 `sources/original-extraction/study/app.beautified.js` 为只读依据，不通过目测猜测修正方向。
- 纸片和地面维持两套独立坐标规则：纸片使用源码的两阶段 X 转换，地面保留既有 Y 转换。
- 新 QA 写入 `app/.artifacts/qa/uv-alignment-2026-08-20/`，不覆盖已接受证据。

## 根因与源码依据

- 原站射线管理器在纸片命中后执行第一次 `uv.x = 1 - uv.x`；每张纸的模拟实例在映射进 `paperBox` 前再次执行同一转换，两次翻转共同适配 GLB 代理网格与可见实例平面的坐标方向。
- 当前复刻版在 `raycastPaper()` 中保留了第一次翻转，但 `mapHitUvToSimulation()` 直接使用翻转后的 X，遗漏第二次转换，因此注入模拟图集的位置关于纸片中心水平镜像。
- 地面在源码中只走独立的 Y 转换，不属于本次缺陷。

## 检查与操作

- 在 `WatercolorView.mapHitUvToSimulation()` 中，仅对 `paper` 命中补回第二次 X 转换；`ground` 映射保持不变。
- 在自动 QA 中增加 UV 坐标契约：纸片命中 `x=0.2` 必须映射为纸片区域内归一化 `x=0.8`；地面相同输入仍必须为 `x=0.2`。
- 检查 1440×900 固定指针轨迹截图，确认轨迹末端和水彩显色位于画面同侧。

## 文件变更

- `app/src/experience/world/WatercolorView.ts`
- `app/scripts/qa-experience.mjs`
- `wiki_memory/当前状态/系统架构.md`
- `wiki_memory/知识/流程/验收基线.md`
- `wiki_memory/日志/2026-08-20-UV对齐修复.md`
- `wiki_memory/日志/MOC_工作日志.md`

## 测试与验证

- `npx tsc --noEmit`: 通过。
- `npm run build`: 通过；仅有既有的单 bundle 超过 500kB 提示。
- `npm run verify:integrity`: 73 项素材通过，原始镜像无改动。
- 五视口 QA: 全部通过，`passed=true`；纸片归一化 X 为 `0.8`，地面归一化 X 为 `0.2`。
- 三个桌面视口笔触直径约 76px、长短轴比 1.00；控制台错误 0，远程资源 0。
- 音频、六标题、Full Paint、Restart、草叶、阴影与 reduced-motion 检查继续通过。

## 问题、结果与下一步

- 水彩交互的左右镜像已修复，地面交互未受影响。
- 候选报告为 `app/.artifacts/qa/uv-alignment-2026-08-20/report.json`；用户视觉接受后再决定是否迁入正式证据目录。
