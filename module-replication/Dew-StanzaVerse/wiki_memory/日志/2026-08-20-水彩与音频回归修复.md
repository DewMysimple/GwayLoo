---
type: log
status: archived
kind: bug
importance: high
updated: 2026-08-20
topic: watercolor-audio-regression
source_logs: []
supersedes: null
---

# 2026-08-20｜水彩与音频回归修复

- 时间: 2026-08-20 13:45:00–14:38:00（北京时间）
- 对话类型: Bug处理
- 主题标签: 水彩笔触、动态模拟图集、音频、显色、图层动画
- 本轮目标: 修复笔触过小与变形、背景音乐未启动、图层立起偏慢及泛白区域无法恢复原色四项问题。
- 日志索引: [[日志/MOC_工作日志|工作日志 MOC]]
- 相关知识: [[当前状态/系统架构|系统架构]]、[[知识/流程/验收基线|验收基线]]、[[知识/规范/来源与完整性|来源与完整性]]

## 已确认的决策

- 继续以 `sources/original-extraction/` 为只读实现依据，不使用损坏的 `experience/` 子路径音频副本。
- 纸片流体模拟从固定等分图集改为按纸片与地面真实尺寸动态打包；26 个区域必须互相隔离。
- 桌面慢速 hover 笔触以 95px 光标为标尺，允许 80%–120% 的可见直径；屏幕投影长短轴比不超过 1.15。
- Enter 是主音乐的明确用户手势入口；主题音量为 1，交叉淡入淡出为 0.8 秒。
- 图层仍从 -90° 倒伏并按 `startAt` 触发，只把立起动画由 7 秒改为 6 秒。
- 本轮 QA 先保存在 `app/.artifacts/qa/refinement-2026-08-20/`，不覆盖已接受证据。

## 检查与操作

- 对照原始 bundle 的模拟区域尺寸、笔刷参数、材质参数、音频启动与 GSAP 时序。
- 将流体管线重构为动态 atlas 的 splat、advection、divergence、pressure、gradient 与 accumulation passes。
- 将命中 UV 映射到纸片自己的 `simulationBox`，按纸片屏幕投影反算笔刷横纵半径，并用连续圆头 SDF 补齐轨迹。
- 恢复 `#e4e4ea`、法线强度 13、法线缩放 3、颜料强度 0.8–1.4；累积颜料保持已经恢复的图集原色。
- 重写音频生命周期，统一管理 Enter 解锁、静音偏好、主题切换、页面可见性和按钮状态。
- 更新自动 QA，删除旧 `radius <= 0.055` 错误条件，增加动态图集、屏幕圆度、颜料累积、6 秒动画和真实音频播放检查。

## 文件变更

- 水彩与交互: `app/src/experience/paint/FluidSimulation.ts`、`PaintManager.ts`、`types.ts`、`world/WatercolorView.ts`、`shaders/fluid.ts`、`shaders/paper.ts`。
- 音频与入口: `app/src/experience/audio/AudioManager.ts`、`dom/LoaderExperience.ts`、`dom/Advantages.ts`、`experience/ExperienceManager.ts`、`main.ts`、`index.html`、`style.css`。
- 验证与兼容: `app/scripts/qa-experience.mjs`、`cdp-smoke.mjs`、`vite.config.ts`、`world/PaintingTitles.ts`。
- 新 QA 产物: `app/.artifacts/qa/refinement-2026-08-20/`（Git 忽略，不是已接受证据）。

## 测试与验证

- `npm ci`: 停止占用 esbuild 的开发服务器后成功重装 16 个依赖包。
- `npx tsc --noEmit`: 通过。
- `npm run build`: 通过，生产 bundle 成功生成。
- `npm run verify:integrity`: 73 项素材全部通过，与只读原始镜像一致。
- 五视口 QA: 1440×900、1920×1080、2560×1440、390×844、430×932 全部通过；无控制台异常、无远程请求。
- 桌面笔触: 三个桌面视口可见直径约 76.02–76.03px，屏幕长短轴比均为 1.00，树冠采样的 pigment 为 1，动态图集 26 区域无重叠。
- 音频: Enter 后 `loop-main` 播放且时间推进，音量达到 1；静音后暂停，恢复后继续推进，切换 `loop-poem` 成功。
- 动画: 26 层无提前立起，立起 tween 为 6.0 秒；滚动、六标题、Full Paint、FAQ、Restart 与 reduced-motion 均通过。

## 问题、结果与下一步

- 本轮四项问题已实现修复并通过自动回归，候选报告为 `app/.artifacts/qa/refinement-2026-08-20/report.json`。
- `evidence/qa/accepted-2026-08-20/` 仍是正式接受基线；只有用户明确验收本轮视觉后，才建立新的证据目录。
- 生产构建仍有单 bundle 超过 500kB 的提示，不影响本轮功能与本地运行，不在本轮精度修复边界内。
