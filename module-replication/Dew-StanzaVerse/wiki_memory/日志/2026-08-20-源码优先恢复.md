---
type: log
status: archived
kind: bug
importance: high
updated: 2026-08-20
topic: source-fidelity-recovery
source_logs: []
supersedes: null
---

# 2026-08-20｜源码优先恢复

- 时间: 2026-08-20（北京时间）
- 对话类型: Bug处理
- 主题标签: 源码还原、流体、地面、草、阴影、滚动、尾部插槽
- 本轮目标: 以只读原始提取包为依据，修复交互死区、显色不一致、地面缺失、草与阴影缺失，并按用户指定改为 `5×` 滚动和空白尾部。
- 日志索引: [[日志/MOC_工作日志|工作日志 MOC]]
- 相关知识: [[当前状态/系统架构|系统架构]]、[[知识/流程/验收基线|验收基线]]、[[知识/规范/来源与完整性|来源与完整性]]

## 已确认的决策

- 所有可恢复行为优先定位到 `sources/original-extraction/study/app.beautified.js`、GLB、图集和字体；原始提取包保持只读。
- `5×` 物理滚动行程与清空尾部内容是用户定制偏差；其余渲染行为尽量逐项对应源码。
- 尾部保留空白区域和 Restart，不保留优势、订阅、礼物、FAQ、页脚文字与交互。
- 新 QA 只写入 `app/.artifacts/qa/source-fidelity-2026-08-20/`，不覆盖已接受证据。

## 源码依据与实现

- 流体：依据 bundle 中 external force、advection、divergence、pressure、gradient subtract、accumulation passes，恢复每实例 FBO/remap、速度、衰减、压力和强度语义；移除自定义持久颜料收敛。
- 命中：用隐藏 GLB 代理和组合地面代理做视锥内最近命中，恢复 paper X 翻转、ground Y 翻转及各自 simulation box 映射。
- 显色：恢复 dry/ink LUT、低对比未交互状态、速度驱动显色与 UV 扰动；普通纸片透明度不再错误乘 reveal alpha。
- 草：依据 bundle 的 PoissonDiskSampling 固定密度实现、1.8/2.8 距离、7 次尝试、椭圆簇、10 片草叶图集坐标、24 段渐变和 Perlin 风动 shader；桌面启用，移动端停用完整实例。
- 阴影：依据 bundle 的 5×5 投影平面、SDF 解码、方向光、spread=3、attenuation=15、skew=0.4 和每层 `castShadow` 标记建立 RenderTarget，并使用投影专用实例矩阵。
- 布局：`ScrollSample.travelMultiplier` 改为 5；尾部替换为固定高度 `.experience-content-slot`，只挂载 Restart。

## 明确推断项

- 没有为流体、图层/地面命中、LUT 显色、草叶分布、草叶 shader 或阴影投影设计替代算法。
- 浏览器/CDP 测试脚本的断线重试和默认截图路径属于本地验证工程实现，不是原站体验行为。
- 空白插槽的具体桌面/移动高度由用户“保留区域”要求确定，原站源码没有该清空后布局。

## 文件变更

- 流体与命中: `app/src/experience/paint/FluidSimulation.ts`、`PaintManager.ts`、`world/WatercolorView.ts`、`shaders/fluid.ts`、`shaders/paper.ts`、`shaders/ground.ts`、`types.ts`。
- 草与阴影: `app/src/experience/world/GrassLayer.ts`、`ShadowProjection.ts`、`app/src/shaders/grass.ts`、`shadow.ts`、`WebGLApp.ts`。
- 滚动与尾部: `app/src/experience/scroll/ScrollController.ts`、`app/index.html`、`app/src/style.css`、`app/src/dom/Advantages.ts`。
- 验证: `app/scripts/qa-experience.mjs`、`app/scripts/cdp-smoke.mjs`。

## 测试与结果

- `npx tsc --noEmit`: 通过。
- `npm run build`: 通过；仅保留单 bundle 超过 500kB 的既有提示。
- `npm run verify:integrity`: 73 项素材通过，原始镜像无改动。
- 五视口 QA: 1440×900、1920×1080、2560×1440、390×844、430×932 全部通过；无控制台错误、无远程资源。
- 滚动: 100px 推进约 0.54 秒镜头；一帧内响应，最大进度滞后 1.5%，停止后归零。
- 流体: 桌面笔触约 76px、长短轴比 1.00，26 个模拟区无重叠，树冠固定轨迹累积采样通过。
- 草: 23 组桌面地面配置；升起采样高于 0.95，3 秒回落采样为 0；移动端不运行完整草实例。
- 阴影: 24 个投影来源、SDF 投影纹理和方向光连接通过；只处理 `castShadow` 图层。
- 内容: 空白尾部区域在五视口保持，且只含 Restart；Full Paint、六标题、音频、Restart 和 reduced-motion 通过。
- 慢速检查: 以 10% timeline 速度检查纸片中间态并截图，确认过渡未提前完成。

## 结果与后续

- 本轮候选报告为 `app/.artifacts/qa/source-fidelity-2026-08-20/report.json`；截图位于同目录各视口子目录。
- `evidence/qa/accepted-2026-08-20/` 仍为正式接受基线；用户明确视觉验收后再迁入新证据目录。
- 原始提取包和旧接受证据均未改动。
