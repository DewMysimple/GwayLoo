---
type: log
status: archived
kind: bug
importance: high
updated: 2026-08-21
topic: layer-rise-reveal
source_logs: []
supersedes: null
---

# 2026-08-21｜图层立起渐进显现

- 时间: 2026-08-21 11:35:00–12:03:30（北京时间）
- 对话类型: Bug处理
- 主题标签: 图层立起、渐进显现、reveal、完整边缘
- 本轮目标: 恢复二维图层从倒伏到立起期间由 0 到 1 的水彩生成，同时保留完成态的完整边缘。
- 日志索引: [[日志/MOC_工作日志|工作日志 MOC]]
- 相关知识: [[当前状态/系统架构|系统架构]]、[[知识/流程/验收基线|验收基线]]、[[知识/规范/来源与完整性|来源与完整性]]

## 已确认的决策

- 原始提取包保持只读；动画依据继续使用 bundle 中的 `uAlpha`、`uCurveCoef`、`rotationZ` 和 `uRevealProgress` 并行动画。
- 保留用户指定的 6 秒立起；源码 15 秒水彩 reveal 继续完整运行。
- 第005次的“完整边缘”只应作用于 reveal 的完成过程和最终态，不能从第 0 秒起覆盖源码前沿。

## 根因与源码依据

- 旋转动画并未丢失，`rotationZ: -π/2→0` 仍在运行；回归发生在片元颜色路径。
- 第005次把普通纸片的 `dryBaseColor` 直接设为完整 `baseColor`，导致仍在更新的 `uRevealProgress: 0→15` 对普通纸片失去可见作用。
- 原始 bundle 的四条并行动画是：瞬时 alpha、10 秒曲面、7 秒旋转和 15 秒 reveal；当前项目仅按既定用户要求把旋转改为 6 秒。

## 检查与操作

- 普通纸片重新以源码 `inkReveal` 作为动画起点。
- 用 `sineInOut(clamp(uRevealProgress / 15))` 作为完整图集接入系数：第 0 秒为 0，第 15 秒为 1。
- 透明背景纸片不接入普通纸片的完整底图规则，继续使用源码 reveal color/alpha。
- QA 的低速检查先重置全部图层，再把 `tree_1` 精确定位到第 3 秒，避免已有时间线与测试时间线互相覆盖。

## 文件变更

- `app/src/shaders/paper.ts`
- `app/src/experience/world/WatercolorView.ts`
- `app/scripts/qa-experience.mjs`
- `wiki_memory/当前状态/系统架构.md`
- `wiki_memory/知识/流程/验收基线.md`
- `wiki_memory/日志/2026-08-21-图层立起渐进显现.md`
- `wiki_memory/日志/MOC_工作日志.md`

## 测试与验证

- `npx tsc --noEmit`: 通过。
- `npm run build`: 通过；仅保留既有的单 bundle 超过 500kB 提示。
- `npm run verify:integrity`: 73 项素材通过，原始镜像未修改。
- 五视口 QA: 全部通过；控制台错误 0、远程资源 0、reduced-motion 通过。
- 1440×900 的 10% 速度第 3 秒状态：`tree_1` 的 `revealProgress=3`，完整底图接入系数约 `0.0955`，不是 0 或 1；截图可见尚未完成的水彩前沿。
- 26 层无提前立起，立起时长仍为 6 秒；音频、笔刷、滚动、草、阴影、标题、Full Paint 和 Restart 回归通过。

## 问题、结果与下一步

- 图层现在重新经历“倒伏 + 水彩从 0 到 1 生成 + 立起”的中间过程；完成后边缘仍显示完整图集信息。
- 候选报告位于 `app/.artifacts/qa/layer-rise-2026-08-21/report.json`，不覆盖正式证据。
