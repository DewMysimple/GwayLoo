---
type: log
status: archived
kind: bug
importance: high
updated: 2026-08-21
topic: layer-edge-integrity
source_logs: []
supersedes: null
---

# 2026-08-21｜图层边缘完整性

- 时间: 2026-08-21 11:19:29（北京时间）
- 对话类型: Bug处理
- 主题标签: 图层边缘、完整底图、dry-LUT、纸片显色
- 本轮目标: 消除所有普通水彩图层边缘永久停留的浅色纸面区域，使图集信息完整显示并保留水彩交互显色。
- 日志索引: [[日志/MOC_工作日志|工作日志 MOC]]
- 相关知识: [[当前状态/系统架构|系统架构]]、[[知识/流程/验收基线|验收基线]]、[[知识/规范/来源与完整性|来源与完整性]]

## 已确认的决策

- 原始提取包保持只读，先以 `study/extracted-shaders/10_fragmentShader.glsl` 和 bundle 为诊断依据。
- 这是用户指定的源码偏差：普通图层始终显示完整的低对比 dry-LUT 底图，不再由四个随机 reveal 圆决定哪些区域有图像。
- 流体继续负责 dry/ink LUT 显色、UV 折射与衰减；唯一透明背景层继续使用源码 reveal 的颜色和 alpha。
- 新证据只写入 `app/.artifacts/qa/layer-edge-2026-08-21/`，不覆盖已接受证据。

## 根因与源码依据

- 图集纹理、mask 与 SDF 均完整，留白不是素材损坏或 alpha 丢失。
- 源码 `computeInkReveal` 只用四个随机圆混合纸面背景与图集色；对长宽比较大的树冠、动物和地景，圆的最终覆盖半径无法到达所有边缘，因此未覆盖区永久显示 `uLighterColor`/场景背景。
- 当前普通纸片仍沿用了这一路径，造成树冠顶部/两侧/树根、牛头/牛身和长地景边缘的系统性白纸区域。

## 检查与操作

- 普通纸片的未交互底色改为完整 `baseColor`，再由 dry LUT 生成原有灰蒙、低对比状态。
- 流体命中仍以 `mixValue` 在 dry/ink LUT 之间过渡，并继续使用速度方向的偏移纹理，水彩显色与水感不受影响。
- 为材质增加 `uCompleteLayerBaseline=1`，自动 QA 在五个视口核对该完整底图契约。
- 透明纸片通过 `vTransparency` 分支继续采用 `inkReveal`，避免破坏背景透明显现。

## 文件变更

- `app/src/shaders/paper.ts`
- `app/src/experience/world/WatercolorView.ts`
- `app/scripts/qa-experience.mjs`
- `wiki_memory/当前状态/系统架构.md`
- `wiki_memory/知识/流程/验收基线.md`
- `wiki_memory/日志/2026-08-21-图层边缘完整性.md`
- `wiki_memory/日志/MOC_工作日志.md`

## 测试与验证

- `npx tsc --noEmit`: 通过。
- `npm run build`: 通过；仅保留既有的单 bundle 超过 500kB 提示。
- `npm run verify:integrity`: 73 项素材通过，原始镜像无改动。
- 五视口 QA: 全部通过，`completeLayerBaseline=1`；控制台错误 0、远程资源 0、reduced-motion 降级通过。
- 8s、24s、44s、55s 场景截图覆盖树、牛/羊、建筑、长地景和透明背景层；与上一候选同帧对比，普通图层的白纸缺口被完整低对比图集内容替代。
- 10% 动画速度截图确认图层立起过程中普通纸片保持完整，透明层行为未被普通纸片规则覆盖。

## 问题、结果与下一步

- 普通图层边缘现在显示完整的原图集信息；画作本身的白色颜料、天空、羊毛及 SDF 透明孔洞仍按素材保留，不伪造不存在的图像。
- 候选报告为 `app/.artifacts/qa/layer-edge-2026-08-21/report.json`；用户视觉接受后再决定是否迁入正式证据目录。
