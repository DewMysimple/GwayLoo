---
type: knowledge
status: active
kind: test
importance: high
updated: 2026-08-24
topic: acceptance-baseline
source_logs:
  - "[[日志/2026-08-20-工程记忆初始化|工程记忆初始化]]"
  - "[[日志/2026-08-21-图层时间与边缘追赶|图层时间与边缘追赶]]"
supersedes: null
---

# 验收基线

当前接受的自动化验收证据位于 `../../evidence/qa/accepted-2026-08-20/`，报告为 `report.json`。

- 视口：1440×900、1920×1080、2560×1440、390×844、430×932。
- 行为：26 个纸片无提前立起；六个悬浮标题可用；Full Paint、FAQ、Restart 与 reduced-motion 降级可用。
- 质量：无控制台异常、无远程资源；滚动约每 100px 推进 0.8–1.2 秒镜头，最大进度滞后不超过 1.5%。

后续 QA 不覆盖此目录，应写入被忽略的 `app/.artifacts/`，只有明确接受的新证据才可迁入 `evidence/qa/`。

## 2026-08-20 精修候选

候选报告位于 `../../app/.artifacts/qa/refinement-2026-08-20/report.json`，尚未迁入正式证据目录。

- 五个标准视口全部通过；26 个动态模拟区域无重叠，26 个图层无提前立起，立起时长为 6.0 秒。
- 三个桌面视口的固定 hover 轨迹笔触直径约 76px，屏幕长短轴比为 1.00，颜料累积采样通过。
- Enter 后主音乐实际播放并推进，音量为 1；静音、恢复与诗歌主题切换通过。
- 生产构建、TypeScript、73 项素材完整性、控制台、远程请求、Full Paint、FAQ、Restart 与 reduced-motion 检查通过。

## 2026-08-20 源码优先候选

候选报告位于 `../../app/.artifacts/qa/source-fidelity-2026-08-20/report.json`，尚未迁入正式证据目录。

- 五个标准视口通过；100px 滚动约推进 0.54 秒镜头，最大进度滞后为 1.5%。
- 26 个动态模拟区域尺寸互异且无重叠；GLB 代理命中、圆形笔刷、显色累积、六标题和 Full Paint 检查通过。
- 桌面建立 23 组源码草地配置；自动检查约 1 秒升至 0.95 以上、离开后约 3 秒回落至 0.05 以下。移动端保持轻量路径。
- 24 个 `castShadow` 来源连接到 `source-shadow-projection` RenderTarget；投影几何、SDF 参数和方向光均取自本地 bundle。
- 尾部空白插槽在各视口保持至少 2.35 屏高度，只含 Restart；原优势、订阅、FAQ 与页脚不可见。
- 10% 动画速度检查确认纸片处于可中断的过渡态；6 秒立起、音频、Restart、reduced-motion、控制台和本地资源检查通过。

## 2026-08-20 UV 对齐修复候选

候选报告位于 `../../app/.artifacts/qa/uv-alignment-2026-08-20/report.json`，尚未迁入正式证据目录。

- 五个标准视口全部通过；纸片坐标契约验证射线阶段的 `x=0.2` 在进入模拟前恢复为 `x=0.8`，补齐原站第二次 X 翻转。
- 地面坐标保持独立规则，测试值仍为 `x=0.2`，没有被纸片修复误翻转。
- 1440×900 固定轨迹截图确认轨迹末端与显色位于画面同侧；不再出现鼠标在右、笔触在左的水平镜像。
- TypeScript、生产构建、73 项素材完整性、五视口 QA、音频、Full Paint、Restart、reduced-motion、控制台与本地资源检查通过。

## 2026-08-21 笔触速度与轨迹衰减候选

候选报告位于 `../../app/.artifacts/qa/brush-motion-2026-08-21/report.json`，尚未迁入正式证据目录。

- 五个标准视口全部通过；三个桌面视口的 `1–3px` 小幅轨迹尺度约为 `0.22–0.23`、可见直径约为 `20.9–22.0px`，严格小于 95px 光标圆圈。
- 三个桌面视口的大幅拖动均到达源码上限尺度 `1.8`，可见作用宽度为 `171px`，即光标范围的 `1.8×`；固定 GPU 采样确认前端点、中点和末端均连续显色。
- 1440×900 衰减采样约为 `0s=1.00`、`1s=0.99`、`2s=0.59`、`3s=0.05`，旧位置持续平滑衰减而非突然清空。
- 10% 慢速分段检查覆盖圆形到不等径胶囊的 11 个过渡采样，轨迹四分点、中点与末段均有连续累积。
- TypeScript、生产构建、73 项素材完整性、音频、Full Paint、Restart、草、阴影、reduced-motion、控制台与本地资源检查通过；控制台错误 0，远程资源 0。

## 2026-08-21 图层边缘完整性候选

候选报告位于 `../../app/.artifacts/qa/layer-edge-2026-08-21/report.json`，尚未迁入正式证据目录。

- 五个标准视口全部通过，26 个普通图层统一启用完整 dry-LUT 图集底图；透明背景层仍保留源码 reveal alpha。
- 与上一候选的同帧截图对比，树冠顶部/两侧/树根、牛身/牛头和长地景边界不再停留为浅色纸面，而是显示连续的低对比水彩内容。
- 10% 图层动画截图确认倒伏到竖立过程中完整底图持续成立；水彩交互仍通过 dry/ink LUT、流体折射和三秒轨迹衰减工作。
- TypeScript、生产构建、73 项素材完整性、音频、Full Paint、Restart、草、阴影、reduced-motion、控制台与本地资源检查通过；控制台错误 0，远程资源 0。

## 2026-08-21 图层立起渐进显现候选

候选报告位于 `../../app/.artifacts/qa/layer-rise-2026-08-21/report.json`，尚未迁入正式证据目录。

- 修复完整底图常量绕过 `uRevealProgress` 的回归：普通图层重新按源码的 15 秒 reveal 前沿逐步生成，完成时再达到完整 dry-LUT 图集。
- 1440×900 的 `tree_1` 在 10% 速度、动画第 3 秒检查时，`revealProgress=3`、完整底图接入系数约 `0.0955`，图层处于明确的 0 与 1 之间；旋转仍处于 `back.out` 的中间/过冲态。
- 26 层触发前仍全部倒伏，立起时长仍为 6 秒；完成态仍保留上一候选修复的完整边缘。
- 五视口、TypeScript、生产构建、73 项素材完整性、音频、笔刷、草、阴影、Full Paint、Restart 与 reduced-motion 全部通过；控制台错误 0，远程资源 0。

## 2026-08-21 图层时间与边缘追赶候选

候选报告位于 `../../app/.artifacts/qa/layer-timing-2026-08-21/report.json`，尚未迁入正式证据目录。

- 26 层继续严格按原始 `startAt` 触发，用户定制时间统一为 3 秒立起、5 秒曲面舒展和 7 秒水彩显色。
- 普通纸片保留源码四点 reveal；完整 dry-LUT 边缘补全在第 6.5 秒达到 1，并在剩余 0.5 秒保持稳定。10% 慢速截图同时覆盖第 1.5 秒中间态和第 6.5 秒完成态。
- 物理滚动行程为 `7.5×`；三个桌面视口的 100px 滚动推进约 0.362–0.363 秒，两个移动视口约 0.387–0.388 秒，最大进度滞后仍为 1.5%。
- 五视口、TypeScript、生产构建、73 项素材完整性、音频、笔刷、草、阴影、标题、Full Paint、Restart 与 reduced-motion 全部通过；控制台错误 0，远程资源 0。
