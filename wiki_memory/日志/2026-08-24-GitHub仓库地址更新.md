---
type: log
status: archived
kind: maintenance
importance: low
updated: 2026-08-24
topic: github-repository-url
source_logs: []
supersedes: null
---

# GitHub 仓库地址更新

## 目标

将本地 Git 远程仓库从旧的 `DewMysimple/Verminoble.git` 指向改名后的 `DewMysimple/GwayLoo.git`。

## 确认的决策

- `origin` 的 fetch 与 push 地址统一使用 `https://github.com/DewMysimple/GwayLoo.git`。
- `Verminoble` 在应用标题、包名、DOM 标识和工程记忆中的引用属于当前应用品牌或内部契约，本次不因仓库改名批量替换。

## 检查范围

- 检查本地 `origin` 地址。
- 搜索仓库链接、项目名称和 `Verminoble` 引用，区分远程地址与应用品牌。
- 使用 `git ls-remote --symref origin HEAD` 验证新远程可访问。

## 变更

- 更新本地 Git 配置中的 `origin` fetch/push 地址。
- 新增本次维护日志。

## 验证摘要

- `origin` fetch/push 均为新仓库地址。
- 远程返回 `refs/heads/main`，访问验证成功。

## 遗留问题和下一步

- 如果后续还要把产品品牌从 `Verminoble` 改为 `GwayLoo`，应另行制定品牌迁移范围，单独更新展示文案、包名、测试标识和工程记忆。

## 待确认长期记忆

- 无。本次仅更新本地 Git 远程配置，不改变应用品牌事实。
