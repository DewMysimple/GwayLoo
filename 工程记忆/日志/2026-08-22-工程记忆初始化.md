---
type: log
status: archived
kind: maintenance
importance: high
updated: 2026-08-22
topic: project-memory-initialization
source_logs: []
supersedes: null
---

# 2026-08-22｜工程记忆初始化

- 时间：2026-08-22（北京时间）
- 类型：`maintenance`
- 状态：`完成`
- 目标：根据外部工程记忆模板，为 Verminoble 建立可持续维护的项目记忆。
- 日志索引：[[日志/MOC_工作日志|工作日志 MOC]]

## 已确认的决策

- 记忆系统完整复制到项目根目录，现有项目 `README.md` 保留不覆盖，记忆说明使用 `工程记忆README.md`。
- 项目主名使用 `Verminoble`；`Dew-StanzaVerse` 和 `David Whyte Experience` 作为别名/基线名称。
- 原始复刻层与 Verminoble 创作层分离。
- 当前保留静态复刻作为可运行基线，后续采用渐进式架构替换。
- 内容、媒体和主题资源优先配置化。
- 当前默认仅限本地实验，公开发布前需要完成素材替换和来源审查。

## 检查与操作

- 读取并采用 `工程记忆构建/AGENTS.md` 的 frontmatter、日志、MOC、链接和 lint 规则。
- 检查当前项目入口、README、目录结构、HTML 引用、主题运行时和资源目录。
- 确认项目是无独立构建工具的静态快照，主体验由 `index.html`、`style.css`、压缩 `app.js` 和 `resources/assets/` 组成。
- 确认运行时依赖 Canvas/WebGL、desktop/mobile 视频纹理、base/over 图层、音频、模型、纹理、LUT、字体和 MSDF 资源。
- 确认 `index.html` 存在多个未导出的相对页面引用，并记录 favicon、Stripe 静态样式和静态后端模块的边界问题。

## 文件变更

- 新增根级 `AGENTS.md`、`工程记忆README.md`、`llm-wiki.md`、`当前状态/`、`决策/`、`知识/`、`日志/`、`模板/` 和 `工具/memory_lint.py`。
- 完善项目概览、系统架构、当前约束、当前待办和已知问题。
- 新增 ADR-001 至 ADR-004，记录基线、创作层、配置化和发布边界。
- 新增体验运行时、内容页面、媒体资源、本地开发、二次创作迁移、资源替换和本地发布边界知识页。
- 调整 lint 工具，使日志 MOC 优先链接 `工程记忆README.md`，不覆盖原项目 README。
- 未修改原始 HTML、CSS、压缩 JavaScript、媒体、模型、字体或第三方运行资源。

## 测试与验证

- 待运行 `python 工具/memory_lint.py index` 更新日志 MOC。
- 待运行 `python 工具/memory_lint.py check` 检查 frontmatter、重复 active 主题、断链和孤儿页面。
- 待通过本地 HTTP 服务确认入口页可返回；缺失页面和未跟踪本地资源作为已知问题保留，不在本轮擅自修复。

## 待确认长期记忆

- Verminoble 的最终品牌名称延展、视觉方向、目标受众和内容策略。
- 是否继续复用现有 WebGL 运行时，还是在配置化完成后迁移到新的组件/构建架构。
- 公开发布时的原创素材清单、授权状态和部署方式。

## 问题、结果与下一步

- 结果：工程记忆已按项目实际结构建立，基线层与创作层边界已记录。
- 遗留问题：原始快照存在缺失子页面和部分本地资源不纳入 Git 的情况。
- 下一步：先完成品牌和视觉方向确认，再从文案与媒体映射配置化开始二次创作。
